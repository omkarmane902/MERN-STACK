// A String Operator is used to join (concatenate) strings together.

// JavaScript mainly uses the + operator for string concatenation.


let firstName = "Omkar";
let lastName = "Mane";

let fullName = firstName + " " + lastName;

console.log(fullName);

